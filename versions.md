# Versions

## 2026-03-18.64
- Блок `Прогноз выручки` сделан сворачиваемым/разворачиваемым (`details`) в едином стиле с блоком `Сезонность`.
- Обновлена версия интерфейса до `2026-03-18.64`.

## 2026-03-18.63
- Блок `Сезонность` сделан сворачиваемым/разворачиваемым (`details`).
- Добавлен новый блок `Прогноз выручки`:
  - выбор периода прогноза (`с даты` / `по дату`),
  - расчет прогноза по дням на основе текущей выборки,
  - модель учитывает среднюю дневную выручку, индекс дня недели, индекс месяца и мягкий тренд последних недель,
  - таблица прогноза по дням + итоговые карточки (всего/среднее/кол-во дней).
- Обновлена версия интерфейса до `2026-03-18.63`.

## 2026-03-18.62
- Исправлена печатная форма/PDF для коротких выборок:
  - блок `Влияние погоды` скрывается, если в текущем диапазоне меньше 3 уникальных дней,
  - блок `Сезонность` скрывается, если в текущем диапазоне меньше 2 уникальных дней.
- Логика скрытия теперь рассчитывается строго по текущему выбранному диапазону дат перед `window.print()`.
- Добавлен отдельный print-флаг `print-hide-seasonality`.
- Обновлена версия интерфейса до `2026-03-18.62`.

## 2026-03-17.60
- Добавлен блок `Сезонность`:
  - анализ по месяцам (`среднее/день`, индекс к средней дневной выручке),
  - анализ по дням недели (`среднее/день`, индекс),
  - сводные карточки: сильный/слабый месяц и сильный день недели.
- В подсказке секции зафиксировано рабочее окно `10:00-23:00` для контекста анализа.
- Обновлена версия интерфейса до `2026-03-17.60`.

## 2026-03-17.59
- В колонке `Погода` оставлена только дневная температура.
- Источник данных погоды сохранен: Open-Meteo (`temperature_2m_max` для дневного значения).
- Убрано усреднение `max/min` для отображения в таблице, чтобы значение соответствовало температуре днем.
- Обновлена версия интерфейса до `2026-03-17.59`.

## 2026-03-17.58
- Убрана кнопка `Скачать debug-log.json` из интерфейса.
- Логирование ошибок и отладочной информации сохранено в фоне (внутренний debug-log продолжает собираться).
- Переименованы пункты `Тип склада`:
  - `Кухня`,
  - `Бар`,
  - `Один склад`.
- Обновлена версия интерфейса до `2026-03-17.58`.

## 2026-03-17.57
- Добавлен фильтр `Показывать влияние погоды`:
  - переключает блок аналитики погоды,
  - состояние сохраняется в `localStorage`,
  - при выключении скрываются погодные колонки и погодный блок.
- Добавлен блок `Влияние погоды` с коэффициентами корреляции:
  - выручка ↔ температура,
  - выручка ↔ осадки,
  - выручка ↔ облачность,
  - показан самый сильный погодный фактор с краткой интерпретацией.
- Добавлено выделение выходных и праздничных дней:
  - в `Итоги по датам` и `Детализация по ресторанам` (подсветка + бейджи),
  - на графике при группировке `По дням` (фоновые маркеры + легенда),
  - в печатной форме PDF (бейджи сохраняются и читаются).
- Обновлена версия интерфейса до `2026-03-17.57`.

## 2026-03-17.56
- Добавлена интеграция погоды по дням выручки в блок `Итоги по датам`:
  - новая колонка `Погода` (иконка + средняя температура + облачность + осадки),
  - погодные данные автоматически подгружаются при изменении выборки.
- Добавлено распределение по городам для погоды:
  - по умолчанию `Нижний Новгород`,
  - для объектов `Циолковского 19а`, `Каспарус`, `Ударник`, `Ленина 64` используется `Дзержинск`.
- Для периодов неделя/месяц погодные данные агрегируются по всем дням периода.
- Обновлена версия интерфейса до `2026-03-17.56`.

## 2026-03-10.55
- Диапазон дат фильтра перенесен из блока `Сравнение периодов` в блок `Фильтры`.
- Поля переименованы в более понятный формат:
  - `Данные: период выборки`,
  - `С даты`,
  - `По дату`.
- В блоке `Сравнение периодов` оставлены только параметры сравнения периодов.
- Обновлена версия интерфейса и `debug-log` до `2026-03-10.55`.

## 2026-03-10.54
- В блоке выбора периодов сравнения переименованы подписи:
  - `Период A` -> `Текущий период`,
  - `Период B` -> `Предыдущий период`.
- Обновлена версия интерфейса и `debug-log` до `2026-03-10.54`.

## 2026-03-10.53
- Компоновка выбора периодов `A/B` в блоке сравнения сделана компактной:
  - оба периода объединены в один горизонтальный переключатель,
  - добавлена визуальная стрелка направления сравнения `A -> B`.
- Улучшен адаптив для узких экранов:
  - переключатель периодов перестраивается в вертикальный вид,
  - стрелка автоматически разворачивается вниз.
- Обновлена версия интерфейса и `debug-log` до `2026-03-10.53`.

## 2026-03-10.52
- Улучшен блок `Сравнение периодов`:
  - добавлен поясняющий текст в заголовке блока,
  - поля сравнения выделены в отдельную визуальную область с рамкой и фоном.
- Улучшена читабельность карточек результатов сравнения:
  - текущий/предыдущий период получили разные акцентные маркеры,
  - карточка `Разница` теперь подсвечивается по направлению (`рост`/`снижение`),
  - добавлены короткие подписи состояния в пустых и отключенных сценариях.
- Обновлена версия интерфейса и `debug-log` до `2026-03-10.52`.

## 2026-03-10.51
- Исправлена компоновка блока `Фильтры`:
  - заголовок `Фильтры` теперь занимает всю ширину панели,
  - `Рестораны` и `Тип склада` выравниваются в одной строке без пустой области слева.

## 2026-03-10.50
- Блок `Исключенные объекты` сделан сворачиваемым внутри `Фильтры`:
  - по умолчанию занимает минимум места,
  - в заголовке показывается счетчик активных исключений (`N из M включено`).
- Улучшена читаемость панели фильтров:
  - основная сетка фильтров переведена в 2 колонки (рестораны и тип склада),
  - исключения вынесены в отдельную аккуратную раскрывающуюся карточку.
- Экспорт PDF теперь получает имя файла с диапазоном дат текущей выгрузки:
  - формат: `Выручка_ресторанов_YYYY-MM-DD-YYYY-MM-DD`.

## 2026-03-10.49
- Блок `Исключенные объекты` перенесен в панель `Фильтры`.
- Удалена отдельная верхняя секция исключений, чтобы интерфейс стал компактнее.
- Обновлены стили для встроенного блока исключений (занимает всю ширину в фильтрах).

## 2026-03-10.48
- Добавлена панель `Исключенные объекты`:
  - можно добавлять пользовательские объекты в исключения,
  - можно включать/выключать исключение чекбоксом,
  - пользовательские объекты можно удалять.
- Исключения сохраняются в `localStorage` и применяются при следующей загрузке файлов.
- При изменении исключений загруженные файлы автоматически пересчитываются.
- Исправлена печать PDF:
  - блок `Сравнение периодов` снова печатается, если сравнение включено,
  - если сравнение выключено, блок сравнения скрывается в печати.

## 2026-03-10.47
- Добавлены новые исключения из выборки и расчетов:
  - `Юность ул. Зеленский Съезд, д. 8/10`,
  - `ИП Амельченко Евгений Андреевич`,
  - `Фудтрак Амельченко пл. Маркина, д. 12А`,
  - `Фабрика кондитерка`,
  - `ул. Большая Покровская, д. 13`,
  - `Швейцария БИК "ПРИСПЕХ"`,
  - `Фабрика пекарня`.

## 2026-03-10.46
- Переключатель `Включить сравнение` перенесен в блок `Сравнение периодов` (убран из блока графика).
- Упрощен интерфейс при отключенном сравнении:
  - скрываются элементы, относящиеся только к сравнению (`Режим сравнения`, выбор периодов A/B, поля предыдущего периода, `Вид сравнения` графика),
  - в карточках сравнения остается только итог текущего периода.
- Блок `Сравнение периодов` исключен из печатной формы PDF.
- Обновлен номер версии в UI и в `debug-log` выгрузке.

## 2026-03-10.45
- Добавлена кнопка `Скачать график PNG`.
- Добавлен переключатель `Включить сравнение`:
  - при выключении сравнения блок `Сравнение периодов` показывает только текущий период,
  - на графике отключаются предыдущий период и режимы сравнения.
- Режимы сравнения графика (`Наложение`, `Рядом`, `100% индекс`) работают только когда сравнение включено.

## 2026-03-10.44
- Добавлен третий вид сравнения графика: `100% индекс`.
- В режиме `100% индекс` оба периода показываются линиями в процентах к первой точке периода (`первая точка = 100%`).
- Tooltip в режиме индекса показывает индекс текущего и предыдущего периода помимо выручки.

## 2026-03-10.43
- Добавлен переключатель вида сравнения графика:
  - `Наложение` (столбцы + пунктир предыдущего периода),
  - `Рядом (2 столбца)` (парные столбцы текущего и предыдущего периода).
- Легенда и отрисовка автоматически подстраиваются под выбранный вид сравнения.

## 2026-03-10.42
- Добавлен переключатель `Группировка графика`:
  - `По дням`,
  - `По неделям`,
  - `По месяцам`.
- Сравнение на графике теперь строится в том же выбранном масштабе (текущий период vs предыдущий).
- Обновлен tooltip графика для группированных периодов (показывает подписи периодов и разницу).

## 2026-03-10.41
- Добавлен интерактивный `hover` на графике:
  - показывает дату, выручку текущего периода, выручку предыдущего периода и разницу.
- Добавлено сравнение периодов непосредственно на графике:
  - столбцы: текущий период,
  - линия: скользящее среднее (7 дней),
  - пунктирная линия: предыдущий период.
- Для графика добавлены подписи `MAX/MIN/СРЕДНЕЕ/ИТОГО`, компактная шкала Y и расширенная легенда.

## 2026-03-09.40
- График на странице сделан более наглядным и информативным:
  - добавлены столбцы дневной выручки,
  - добавлена линия скользящего среднего за 7 дней,
  - добавлены сетка и подписи оси Y в компактном формате,
  - добавлены метки `MAX`, `MIN`, `СРЕДНЕЕ`, `ИТОГО`,
  - добавлена легенда и более равномерные подписи по оси X.

## 2026-03-09.39
- Оптимизирована структура интерфейса:
  - добавлен заголовок секции `Фильтры`,
  - блок управления таблицей разбит на 2 логические строки (склады / сортировка+группировка+экспорт),
  - улучшено поведение кнопок экспорта и селектов на узких экранах.
- В печатную форму PDF возвращен блок `Сравнение периодов`:
  - в печати выводятся карточки сравнения,
  - скрыты только поля ввода периода внутри блока для более чистого отчета.

## 2026-03-09.38
- Добавлены кнопки для массового управления детализацией складов:
  - `Развернуть все склады`,
  - `Свернуть все склады`.
- Кнопки автоматически блокируются, если выключен режим `Показывать склады внутри ресторана`.
- Улучшена печатная форма PDF:
  - скрыты технические и лишние блоки (загрузка, фильтры, сравнение периодов, график, элементы управления),
  - уменьшены шрифты и отступы для большей вместимости,
  - улучшены стили таблиц и карточек показателей для более чистого вида.

## 2026-03-09.37
- Исправлено обновление блока `Итоги по датам` при смене селекта `Группировка`.
- Теперь при переключении `По дням / По неделям / По месяцам` одновременно обновляются:
  - `Итоги по датам`,
  - `Детализация по ресторанам`.

## 2026-03-09.36
- Блок `Итоги по датам` синхронизирован с переключателем `Группировка`:
  - `По дням`,
  - `По неделям`,
  - `По месяцам`.
- Теперь итоги в таблице и детализация считаются в одном выбранном периоде.

## 2026-03-08.35
- Добавлена группировка в блоке `Детализация по ресторанам`:
  - `По дням`,
  - `По неделям` (диапазон недели),
  - `По месяцам`.
- При смене группировки таблица пересчитывается с учетом текущих фильтров и раскрытия складов.

## 2026-03-08.34
- Добавлен экспорт в Excel в формате:
  - первый столбец: рестораны (в столбик),
  - отдельные столбцы по датам с выручкой,
  - первая строка данных: `Итого` по всем ресторанам.
- Экспорт учитывает текущие выбранные фильтры.

## 2026-03-08.33
- Добавлен автодеплой на GitHub Pages через GitHub Actions (`.github/workflows/pages.yml`).
- Добавлен файл `.nojekyll` для корректной публикации статических файлов.
- Версия интерфейса обновлена до `2026-03-08.33`.

## 2026-03-08.32
- Исключены из выборки и расчетов рестораны: `Шале №15`, `Совнаркомовская 13`.

## 2026-03-08.31
- В блок сравнения добавлен выбор конкретных периодов `Период A` и `Период B` для режимов:
  - неделя к неделе,
  - месяц к месяцу,
  - год к году.
- Для пользовательского режима сохранен ручной выбор предыдущего периода по датам.

## 2026-03-08.30
- Поля выбора текущего периода (`С даты` / `По дату`) перенесены из общего блока фильтров в блок `Сравнение периодов`.
- Выбор периода теперь логически сгруппирован в одном месте с настройками сравнения.

## 2026-03-08.29
- Добавлен блок `Сравнение периодов`.
- Доступны режимы сравнения: `Неделя к неделе`, `Месяц к месяцу`, `Год к году`, `Пользовательский`.
- В блоке выводятся: выручка текущего периода, выручка предыдущего периода, разница и процент изменения.

## 2026-03-08.28
- Убрана экранная отладочная строка из интерфейса (оставлен только функционал выгрузки логов).
- Добавлено автоматическое логирование непойманных ошибок (`window.error`, `unhandledrejection`) в `debug-log.json`.
- Кнопка `Скачать debug-log.json` используется как единая точка просмотра диагностики при ошибках.

## 2026-03-08.27
- Убрана отладочная информация из интерфейса.
- Добавлено логирование в отдельный файл `debug-log.json` (кнопка `Скачать debug-log.json`).
- Ошибки и ключевые события парсинга сохраняются в локальный журнал браузера (localStorage) и выгружаются в JSON для анализа.

## 2026-03-08.26
- Исправлено отображение в PDF:
  - убраны `sticky`-заголовки таблиц в печатном режиме (чтобы не было наложения строк),
  - уменьшены шрифты и отступы для большей плотности данных,
  - добавлены правила переноса страниц (`page-break-inside`) для аккуратной разбивки,
  - скрыт график при печати, чтобы в PDF помещалось больше табличных данных.

## 2026-03-08.25
- Добавлена сортировка блока `Детализация по ресторанам`:
  - по выручке (больше -> меньше),
  - по имени ресторана (А -> Я),
  - по имени ресторана (Я -> А).
- Добавлена кнопка `Экспорт в PDF` (через печатный режим `window.print`).
- Улучшены печатные стили для корректной выгрузки в PDF.

## 2026-03-08.24
- Добавлен файл `versions.md` для ведения версии и истории изменений.
- Установлено правило: каждое следующее изменение фиксируется в этом файле.
- Подготовлена выгрузка обновленной версии в GitHub для проверки с телефона.

## 2026-03-08.23
- Мобильная оптимизация интерфейса: улучшены сетки фильтров, размеры отступов и таблиц для телефона.

## 2026-03-08.22
- Улучшено отображение фильтров: устранено растягивание блоков на средних экранах.

## 2026-03-08.21
- Исправлено смешивание выручки между складами из-за блочных исключенных заголовков.

## 2026-03-08.20
- Объединены в одну группу: `Самурай, Гагарина, 35` и `Самурай, Парк Швейцария`.

## 2026-03-08.19
- Фильтр `Тип склада` переведен в мультивыбор.

## 2026-03-08.18
- Добавлен фильтр по типу склада: `Все`, `Кухня`, `Бар`, `Один склад`.

## 2026-03-08.17
- Возвращены исходные названия групп с `Самурай` для доставочных точек.

## 2026-03-08.16
- Усилена нормализация имен: исключение `БУРГЕР БИК ... Чайка`, объединение `ВИНЕДО`.

## 2026-03-08.15
- Исключены из выборки `БУРГЕР БИК ООО Чайка`, объединены `ВИНЕДО БАР/КУХНЯ`.

## 2026-03-08.14
- Исключены `Бургер Бик` и `Фабрика разделка`.

## 2026-03-08.13
- Исключен `Основной склад`.
- Итоги по датам считаются по выбранным ресторанам.

## 2026-03-08.12
- Добавлен блок `Итоги по датам`.
- Объединены группы `Винедо` и `Самурай, Веденяпина, 1А`.

## 2026-03-08.11
- Исключены `Онлайн оплата, СБП`, `НТО ООО Приспех ...`.
- Объединены `Самурай, Октября, 2` и `Самурай, Октября, д. 2`.

## 2026-03-08.10
- Добавлен переключатель отображения складов в таблице.
- Добавлены группы для односкладных точек.

## 2026-03-08.9
- Добавлен мультивыбор ресторанов.
- Добавлена иерархия `ресторан -> склады` с раскрытием.

## 2026-03-08.8
- Добавлен парсер для формата `Периодичность по дням`.

## 2026-03-08.7
- Исправлено падение при `rangeInfo = null`.

## 2026-03-08.6
- Добавлено распределение суммы по дням, если отчет содержит только диапазон без дневной детализации.

## 2026-03-08.5
- Исправлен битый диапазон `!ref` в Excel перед парсингом.
